node_modules/
dist/
backend/uploads/*
!backend/uploads/.gitkeep
.env
*.log
.DS_Store

# Firebase app config is project-specific (contains API keys). Not committed;
# builds fail clearly until google-services.json is supplied (see app/build.gradle).
mobile/android/app/google-services.json
mobile/google-services.json

# Local APK artifacts
*.apk

# Generated web-push service worker (rebuilt from scripts/firebase-messaging-sw.tpl.js
# by `npm run prebuild` / `predev`, which inject the FCM web messaging sender id).
frontend/public/firebase-messaging-sw.js
